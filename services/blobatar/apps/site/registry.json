{
  "$schema": "https://ui.shadcn.com/schema/registry.json",
  "name": "blobatar",
  "homepage": "https://blobatar.dev",
  "items": [
    {
      "name": "avatar",
      "type": "registry:ui",
      "title": "Blobatar",
      "description": "An Avatar that falls back to a deterministic geometric blobatar generated from the user's name.",
      "author": "Alain <https://blobatar.dev>",
      "categories": ["avatar"],
      "//dependencies": "Unpinned on purpose. Every package in this set publishes the same version, so `latest` of both resolves to a matched pair — which is what `@blobatar/react`'s exact-major peer range on `blobatar` requires. A `^2` written here would instead freeze this item at gen2 the moment gen3 ships, and a registry item nobody rebuilds is exactly how that goes unnoticed.",
      "dependencies": ["blobatar", "@blobatar/react"],
      "registryDependencies": ["avatar"],
      "files": [
        {
          "path": "registry/blobatar.tsx",
          "type": "registry:ui",
          "target": "components/ui/blobatar.tsx"
        }
      ]
    }
  ]
}
