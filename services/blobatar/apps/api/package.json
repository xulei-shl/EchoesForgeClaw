{
  "name": "blobatar-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "check": "bun test"
  },
  "//dependencies": "Both generations come from npm package majors: the current `blobatar` normally links to this workspace, while `blobatar-v1` aliases the frozen v1 release for `?gen=1`. A clone made by the Deploy to Cloudflare button installs this directory alone, so workspace protocols would be unresolvable.",
  "dependencies": {
    "blobatar": "^2.4.0",
    "blobatar-v1": "npm:blobatar@^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4"
  }
}
