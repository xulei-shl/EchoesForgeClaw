fastapi
uvicorn
sqlalchemy
pydantic
pydantic-settings
PyJWT
passlib[bcrypt]
bcrypt==4.0.1
python-multipart
httpx
# openai-agents 0.20.0 硬性要求 openai>=2.45.0,<3（不支持 3.x，升 3.x 会破坏 Skill Agent）；
# 2.54.0 是 2.x 最新。升级 SDK 前必须先跑契约测试：
#   PYTHONPATH=. .venv/bin/python tests/test_skill_agent_stream.py
#   PYTHONPATH=. .venv/bin/python tests/test_sandbox_hardening.py
# （事件白名单 / FunctionTool 形态 / 工具参数提取均依赖 SDK 内部形状）
openai==2.54.0
openai-agents==0.20.0
sse-starlette
alembic
