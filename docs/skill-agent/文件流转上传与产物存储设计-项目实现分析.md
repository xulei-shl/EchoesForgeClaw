# 《AI Agent 里的文件流转:上传与产物存储的完整设计》分析与项目实现对比报告

- **日期**: 2026-09-04（§9 修订于 2026-09-06）
- **分析对象**: `docs/skill-agent/AI-Agent里的文件流转上传与产物存储的完整设计.md`
- **对比基线**: BookForge 项目实际实现（pi-coding-agent 0.84.2，RPC 模式）
- **适用范围**: 开发人员（后端 `backend-ts` / 前端 `frontend`）
- **结论先行**: 设计文档的核心原则合理，且与 pi 的实际架构高度一致；项目实现已落地文档推荐的主要做法（差分产物检测、大文件走工具、路径白名单）。真正值得做的优化有两项：① 水合时把 manifest 检测到的产物绑定到消息卡片（补上「shell 写文件被漏」的展示缺口）；② 去掉每轮双次同步工作区遍历。**①② 已于 2026-09-04 实施完成（见第七章）**；③ 文件上传产品决策亦于同批落地（见第八章）。对象存储 + file_id + DB 是「横向扩展时才需要」的触发器，不是当前单机部署的缺陷。

---

## 一、设计文档本身是否合理

### 1.1 合理且与 pi 架构一致的部分

- **核心主线「文件本体永远存放在上下文之外，对话里流动的只是引用，需要时按引用取回」** —— 这正是 pi 的模型（工作区落盘 + `read`/`grep`/`bash` 工具按需读取），原则正确。
- **产物检测推荐方案 (c)「约定目录 + diff 检测」** —— 正确，它是唯一能捕获 shell 命令写入文件的方式，不依赖模型自觉。
- **安全红线**（worker 侧执行 I/O、basename/路径白名单、大文件不整块回灌、HTML 渲染用成熟库消毒）—— 全部合理。

### 1.2 有条件成立、并非普适的部分

| 文档断言 | 适用前提 | 本项目现状 |
| --- | --- | --- |
| §2.3「分布式平台必须用对象存储 + file_id + 入库」 | 多 worker / 横向扩展 / 沙箱临时回收 | 单机部署，本地磁盘寻址完全够用 |
| §3.2 目录树 + 懒加载 + 虚拟滚动 | 整个项目（成百上千文件）的工作区 | 素材生成平台，每工作区文件很少，扁平列表即可 |
| §3.3 大文本分页 / 媒体 Range 端点 | 工作区出现几十 MB 日志 | 目前产物为小文件，流式整发即可 |

> 结论：文档是一份面向「通用 Agent 平台 / 代码生成平台」的架构蓝图，其中关于分布式存储、目录树、渲染矩阵的部分对当前部署属于过度设计，但对将来扩展有明确的指导价值。

---

## 二、pi-agent 的实现方式（对照文档）

> 代码基线：`docs/skill-agent/pi-main/packages/coding-agent/src`（与安装的 `@earendil-works/pi-coding-agent@0.84.2` 一致）

### 2.1 上传 → 递给模型

| 文档类型 | pi 实现 | 与文档对照 |
| --- | --- | --- |
| 图片 | `@file` CLI（`cli/file-processor.ts`）→ base64 `ImageContent` 块，自动缩放至 2000×2000（`processImage`）；RPC 模式经 prompt 命令 `images` 字段直传 | 匹配「图片多模态块」的 base64 变体；文档更推崇 URL/file_id 引用（见 §4.4 限制） |
| 小文本 | `@file` CLI → 以 `<file name="…">…</file>` 块**拼进 user 消息** | 匹配「小文本注入 user 消息」；项目侧没有文本上传，不适用 |
| 大文件 | 工作区落盘 + `read`（`core/tools/read.ts`，offset/limit 分页、行/字节双截断 + `Use offset=N to continue` 提示）+ `grep`/`find`/`ls`/`bash` | **完全匹配** §1.6「给路径 + 给工具，模型多轮读取」 |
| 上下文文件 | `--no-context-files` 关闭自动发现；项目改用 `--append-system-prompt AGENTS.md` 显式注入 | AGENTS.md 属配置级稳定内容，不破坏 prompt cache，文档 §1.5 的缓存警告不适用 |

**关键约束**：`@file` 在 RPC 模式下被拒绝（`main.ts:638`），因此项目必须把图片经 RPC prompt 的 `images` 字段传给 pi（`runner.ts` 组装 `{type:'prompt', message, images:[{type:'image',data,mimeType}]}`），这是协议决定的路径，不是可选项。

### 2.2 产物：pi 本身没有检测机制

pi 内部**不存在**任何产物检测/捕获逻辑：

- agent 写出的文件只是磁盘文件，模型在正文里用路径引用；
- `read` 工具读到的图片以 base64 内联进工具结果，再进入会话历史（`utils/tool-result-images.ts` 只做缩放归一化，不做去重/入库）。

对照文档 §2.2 的三种方式：pi 原生等于「(b) 模型声明」的弱化版（连 `MEDIA:` 标记都没有，只是正文里裸写路径）。**项目在 pi 之外补上了 (c) 差分检测**——这正是集成做对了的地方。

---

## 三、项目前后端实际交互（现状梳理）

### 3.1 上传方向（用户 → 模型）

```
前端 PiChatNodeHost（Skill Agent：附件上传 → inputs/ 路径引用；base64 兜底 ≤4 张）
  → POST /api/modules/bookplate/chat { message, images, skills, workspace_id }
  → 后端 preparePiWorkspace（AGENTS.md 软链 / skills 装配 / models.json / settings.json）
  → pi RPC 子进程 prompt 命令 images 字段 → user 消息 ImageContent → chat.jsonl
```

- 经 base64 通道的图片（外部 URL 兜底、`read` 工具读图、LLM / FastClaw 模式）会**常驻会话历史**（chat.jsonl），会话存续期间每次 provider 请求都会重发（见 §4.4）。
- Skill Agent 模式已支持**任意格式附件**：`POST /chat/upload` 落盘 `{ws}/inputs/`、`POST /chat/import` 继承图片路径化，消息以 `inputs/<name>` 路径引用（见 §8）——不再只是「文本 + 图片」；LLM / FastClaw 模式仍走 base64 图片（≤4 张，`MAX_CHAT_IMAGES`）。
- 请求体上限已提到 50MB（`server.ts` bodyLimit，适配 base64 膨胀约 33%；附件每请求限额同 50MB）。

### 3.2 产物方向（模型 → 用户）

```
每轮开始 snapshotWorkspace(ws)（readdirSync/statSync 全量递归）
  → pi 执行一轮（工具写文件、生图 outputs/、bash 等）
  → 每轮结束再 snapshotWorkspace → diffWorkspace（mtime+size，排除 .agents/.pi/.pi-agent/inputs/AGENTS.md）
  → agent_file SSE 事件（transient，不落库） + .pi-agent/artifacts.jsonl（append-only manifest）
```

- **文件面板**：`GET /chat/files` = 当前快照 ∪ manifest 历史（已删文件保留 `exists=false`）。
- **下载/预览**：`GET /skill-files?path=&workspace_id=` —— 鉴权 + `resolveSkillAbs` 双层白名单（词法 normpath + realpath 穿透，仅放行工作区 / skills / agents 根），流式发送、正确 Content-Type、RFC 5987 文件名。
- **刷新后的消息卡片**（水合 `pi-session-hydrate.ts`）：来自 ① 内联图片块 → 鉴权图片卡；② `write`/`edit` 工具调用参数推导；③ 前端对正文的路径正则提取（`workspaceFiles.ts`，把 `/…/runtime/{uid}/workspace/{wsId}/outputs/x.png` 换算为 skill-files URL）；④ manifest 差分产物按轮末 `ts` 归属绑定到产生它的 assistant 消息（§7.2 实施完成）。
- **manifest 双用途**：文件面板（当前快照 ∪ 历史，已删文件标 `exists=false`）与消息卡片水合（按轮末 `ts` 归属，§7.2）共用同一份 `.pi-agent/artifacts.jsonl`。
- FastClaw 模式：从工具结果正则收割路径 → 拷入 `outputs/` → 复用同一套 agent_file 管线，并同步写 manifest（服务端可再到达，与水合绑定同构；近似文档 (a)）。

### 3.3 存储布局（全部本地磁盘）

```
runtime/{uid}/workspace/{wsId}/       单个 chat 节点工作区（软链装配 + agent 产物）
runtime/.agent/skills/、runtime/.agent/agents/{id}/AGENTS.md
runtime/{uid}/skills/                 用户已安装 skill 登记
```

无对象存储、无 files/artifacts 表；寻址 = `workspace_id + 工作区相对路径`（拼进 URL）。注意 `runtime/` 内含每工作区 API Key（models.json / settings.json / web-search.json），已 gitignore，但需持久卷 + 备份策略。

---

## 四、设计文档 ↔ 项目实现对照表

| 文档章节 | 项目实现 | 结论 |
| --- | --- | --- |
| §1.2 上传先落存储拿 file_id | 工作区相对路径 + workspace_id，无 file_id / files 表 | 单机部署够用；扩展时是迁移点 |
| §1.4 图片多模态块 | RPC `images` 字段 → base64 ImageContent | 匹配 base64 变体；文档更优的「URL/file_id 复用」受协议限制 |
| §1.5 小文本进 user 消息（非 system） | 无文本上传；AGENTS.md 追加进 system prompt | 不适用；AGENTS.md 配置级稳定，缓存警告不生效 |
| §1.6 大文件落沙箱 + 工具读取 | pi read/grep/bash + offset/limit 分页截断 | **完全匹配** |
| §1.7 红线：base64 不驻留历史 | pi 把图片 base64 存进 chat.jsonl，每轮重发 | 真实偏差，协议绑定（§4.4） |
| §2.2 (c) diff 检测 | snapshotWorkspace + diffWorkspace（mtime+size、排除前缀、无全量 hash） | **匹配且为推荐方案** |
| §2.3 分布式必须对象存储 | 单机本地磁盘 + 路径寻址 | 现在正确；文档论证即将来迁移触发点 |
| §2.4 artifacts 表挂 message_id | jsonl manifest 无 message_id；按轮末 ts 归属绑定到消息卡片（§7.2 已实施） | 缺口已补齐；无 message_id 是格式取舍，功能等价 |
| §2.5 项目工作区 / 网络盘 / 快照 zip | 不适用（素材生成平台，非代码生成） | — |
| §3.1 成熟渲染库 + DOMPurify | streamdown（React 渲染 + shiki 高亮 + 消毒管线；仅 mermaid SVG 走 dangerouslySetInnerHTML） | 合理；markdown → React 元素，无原始 HTML 注入面 |
| §3.2 目录树 + 懒加载 + 虚拟滚动 | 扁平文件列表 | 当前规模够用，暂缓 |
| §3.3 大文本分页 / 媒体 Range | skill-files 整文件流式发送，无 Range | 次要；工作区长大会话日志后再做 |

---

## 五、优化建议

### 5.1 值得做（当前收益最高）

> 本节三条均已实施：①② 见 §7（2026-09-04），③ 见 §8（2026-09-04）。以下保留原始建议供追溯。

**① 水合时把 manifest 检测到的产物绑定到消息卡片（补「shell 写文件被漏」缺口）**

- 现状：`bash python make_report.py` 生成的文件（文档 §2.1 的场景）进了 manifest 和文件面板，但**不会出现在聊天消息的文件卡片里**，除非模型恰好在正文写了路径（前端正则）或用了 `write`/`edit`（水合推断）。
- 做法：差分时在 manifest 记录产生该产物的轮次/时间戳（或 diff 时直接写 entry-id），水合时按「上一条消息时间戳之后新增/变更」把 manifest 条目挂到对应 assistant 消息的 `files` 上。
- 涉及：`backend-ts/src/services/pi/snapshot.ts`、`pi-session-hydrate.ts`。
- 验证：刷新后 bash 生成的产物出现在消息卡片（现有 `pi-session-hydrate.test.ts` 可加用例）。

**② 去掉每轮双次同步工作区遍历**

- 现状：`runner.ts` 每轮首尾各一次 `snapshotWorkspace`，内部是 `readdirSync`/`statSync` 全量递归——**同步阻塞后端事件循环**，多租户并发 + 大工作区时影响全服。
- 做法：单次遍历同时产 before/after（首轮浅层采集或首尾合并遍历）；或改异步遍历。
- 说明：`pi-agent-optimization-plan-2026-09-02.md` 的 P3-10 已标记该项但未实施。
- 验证：产物差分行为回归（`pi-agent-run.test.ts` / 手工验证 agent_file 卡片）。

**③ 产品决策：是否支持任意文件上传**

- 现状：聊天只收「文本 + 图片」，文档 §1 的「小文本 / 大文件」两条路没有入口。
- 若产品要支持挂载文档/代码/日志：文档蓝图（先落存储 → file_id → 小文本注入 user 消息 / 大文件落工作区给工具）正好适用，pi 的工具半截（工作区 + read/grep/bash）已就绪，只需补上传与落盘侧。

### 5.2 pi 协议绑定（记录成本，不做对抗性改造）

**④ base64 常驻会话历史导致的 token 成本**

- 用户首轮图片与 `read` 工具读到的图片都会以 base64 存进 chat.jsonl，会话内每轮 provider 请求重发，直到压缩。
- RPC 协议只接受内联 base64 `ImageContent`，无 URL/file_id 附件通道，不改 pi 无法根治。
- 现有缓解：2000×2000 自动缩放 + 每轮 4 张上限。可进一步把聊天附件缩放目标调小（如 1024–1568px），但成本是集成固有的。

### 5.3 暂缓（按需再做）

| 项 | 触发条件 |
| --- | --- |
| 对象存储 + file_id + files/artifacts 表 | 多 worker / HA / 沙箱回收（文档 §2.3 论证即迁移依据）；迁移前至少保证 runtime/ 持久卷 + 备份 |
| `skill-files` 增加 Range / 文本分页 | 工作区出现大日志等大文件 |
| 目录树 + 懒加载 + 虚拟滚动 | 工作区达到代码仓库规模 |

---

## 六、总结

- 设计文档的原则合理，且与 pi 架构高度一致；项目已落地**文档推荐的核心项**：差分产物检测 (c)、大文件走工具、所有文件端点鉴权 + 路径白名单、服务端会话真相源、流式下载。
- 优化优先级：**① 水合绑定 manifest 产物到消息** > **② 单次/异步工作区遍历** > **③ 文件上传产品决策**。①② 已实施（见 §7），③ 已实施（见 §8）。
- 文档里其余断言（对象存储、file_id 表、目录树、Range 端点）属于「扩展/规模触发项」，不是当前实现的缺陷——不要提前建设。

---

## 七、优化实施记录（2026-09-04）

> 本段记录 ① ② 两项优化的实际落地：改动文件、实现要点、测试与验证结果。编码原则：KISS、模块化、高内聚低耦合、单文件不过度膨胀。

### 7.1 ② 单次工作区遍历（每轮 2 次全量 walk → 1 次）

**`backend-ts/src/services/pi/snapshot.ts`**

- `walkWorkspace` 现在**直接剪枝差分排除目录**（`.agents/` `.pi/` `.pi-agent/` `inputs/`），不再递归进入最大的几棵树（装配的 skills 软链目标、会话 `chat.jsonl`、产物 manifest、已上传图片）。由于这些路径在 diff 阶段本就被排除，剪枝前后结果完全一致，只是少走了遍历。
- 新增工作区基线：`PI_BASELINE_REL`（`.pi-agent/snapshot.json`）、`readWorkspaceBaseline(ws)`（缺失/损坏 → `null`）、`writeWorkspaceBaseline(ws, stamps)`（best-effort，与 manifest 同风格，失败静默）。

**`backend-ts/src/services/pi/runner.ts`**

- 轮开始：`readWorkspaceBaseline(ws) ?? snapshotWorkspace(ws)` —— 仅首轮或基线缺失时才真遍历，语义与旧「轮首遍历」完全一致。
- 轮结束：一次遍历产出 after，随后 `writeWorkspaceBaseline(ws, after)` 落盘。
- 基线在**磁盘**上，进程被杀/重启后依然存在——重启后既有产物**不会被重复上报为 `agent_file`**（附带修正了旧实现的这一隐性行为）。

### 7.2 ① 水合时把 manifest 产物绑定到消息卡片

**`backend-ts/src/services/pi/snapshot.ts`**

- 新增 `readArtifactManifest(ws)`：容错 JSONL 读取器，`ts` 字段对旧行可缺省；`listWorkspaceArtifacts` 改为复用（删除重复解析）。

**`backend-ts/src/services/pi-session-hydrate.ts`**

- 历史构建完成后，把 manifest 条目绑定到**产生它的那条 assistant 消息**上：manifest `ts`（轮末 diff 时刻）落在该轮 assistant ts 与下一轮 user ts 之间，因此绑定到满足 `ts <= entry.ts` 的最后一条 assistant。
- 仅绑定**当前仍存在**的文件；卡片按路径与 `write`/`edit` 推断卡、内联图片卡**去重**。
- **无需改前端**：水合后的 `files` 直接走既有 DTO 通道渲染。

> 行为说明：同一轮里某文件既被 `write` 又被 diff 检测到时，`write` 卡胜出（保留其 `size: 0`）；仅 manifest 检测到的文件带真实大小。如需在重叠场景下改用 manifest 的真实 size，是一行改动。

### 7.3 测试与验证

- `pi-session-hydrate.test.ts`：+2 用例 —— 多轮绑定 + write 卡去重 + 已删文件跳过；旧版无 `ts` 行不参与绑定（list 合并仍兼容）。
- `pi-agent-run.test.ts`：+1 e2e 用例 —— 第 1 轮生成产物并断言基线文件存在 → 杀进程 → 第 2 轮**不再上报 `agent_file`**（基线跨重启存活）。
- `npx tsc --noEmit` → 0 错误。
- 全量 `tests/api/pi-*` 回归 → **85/85 通过**（含 reuse、SSE wire、widgets、web-access 套件）。

### 7.4 遗留项

- ③ 文件上传产品决策：已由 §8 落地（任意格式附件 + @ 引用），本条关闭。
- ④ base64 常驻会话历史的 token 成本：RPC 协议绑定，未做对抗性改造。

---

## 八、任意文件附件与 @ 路径引用实施记录（2026-09-04）

> 本段记录 §5.1 ③「文件上传产品决策」在 Skill Agent（pi）模式的落地：**任意格式文件（含图片）
> 上传 / 上级继承，统一落盘工作区 `inputs/`，对话消息只传路径**；同时在前端输入框实现 `@`
> 模糊检索 + Tab 补全。编码原则：KISS、复用既有工作区/路径白名单设施、SSRF/穿越防线在服务端。

### 8.1 后端：两个端点 + 一处列表扩展

**`POST /api/modules/bookplate/chat/upload`**（multipart，字段名 `file`，`workspace_id` 走查询参数）

- 任意格式（不再限图片），每请求 `limits.fileSize` 覆盖全局 6MB（上限 50MB，对齐 bodyLimit）；
- `saveInputFile`（`file-utils.ts` 新增）：文件名清洗（basename / 控制字符 / 长度截断）+ 同名去重
  （`name (1).ext`）→ 写入 `{ws}/inputs/`，返回 `{ name, path: inputs/<name>, mime, size }`。

**`POST /api/modules/bookplate/chat/import`**（JSON：`{ workspace_id, urls }`）

- 上游继承图片：`resolveStaticImportPath` 白名单（`/static/generated|search-images|map-posters|map-arts/{uid}/{file}`
  与 `/static/covers/{file}`）→ 服务端拷贝进 `inputs/`；其余 URL（data URL / 外部 / API 路径）一律 400，
  防 SSRF 与目录穿越。data URL / 封面代理等非静态来源仍走既有 base64 `images` 通道。

**`GET /chat/files?include_inputs=1`**

- `listWorkspaceArtifacts` 新增 `includeInputs` 选项：`inputs/` 上传文件并入列表（默认不出现——
  上传文件不是 agent 产物，产物面板口径不变）；`@` 引用检索的数据源。

### 8.2 前端：附件通道切换 + @ 引用检索

**附件按钮（Skill Agent 节点）**：`ChatNode` 按 `mode==='skill_agent' && onUploadFile` 分支——

- 任意格式（accept 不限、图标切换 Paperclip）；选中即上传（`PiChatNodeHost.handleUploadFile`，与 send
  同款工作区创建/持久化逻辑），路径**直接插入草稿文本**（chips 仅展示/移除，移除同步删草稿中的路径段）；
- 消息发送时无需额外字段——路径已在正文中（`chat对话中传入路径即可`），pi 以工作区 cwd + `read`/`ls`
  工具按需读取；图片同理（pi read 工具对图片以 base64 内联进工具结果，多模态可用）。
- LLM / FastClaw 模式行为不变（图片 base64）。

**首轮继承图片**（`PiChatNodeHost.send`）：上下文块中的 `/static/` 图片 → `/chat/import` 拷入 `inputs/`，
以 `【继承图片】\n- inputs/x.png` 块注入 wireText（置于上下文之前，`stripInjectedContext` 的通用前缀
规则可剥离）；非静态来源继续走 base64。导入失败降级为 base64，不阻断对话。

**`@` 引用检索**（`ChatNode`，skill_agent 且已有 workspaceId 时启用）：

- 输入 `@` 触发（最近一个 `@` 后无空白）；子序列模糊匹配（连续/前缀加分）过滤 `GET /chat/files?include_inputs=1`
  的路径；↑↓ 选择、**Enter / Tab 补全**（替换 `@…` 为 `inputs/<file>` + 空格）、Esc 关闭、鼠标悬停/点击同效；
- 弹层 portal 到 body（锚定 textarea 上方，避开节点滚动容器裁剪）；结果按 workspaceId 缓存，上传新文件后失效。

### 8.3 测试与验证

- 新增 `tests/api/pi-file-attach.test.ts`：12 用例 —— 上传落盘 / 文件名清洗防穿越 / 同名去重 /
  >6MB 每请求限额覆盖 / import 白名单拷入 / 非法 URL（外部、data URL、`..`）400 / include_inputs 列表。
- `npx tsc --noEmit`（backend）+ `npx tsc -b`（frontend）→ 0 错误。
- 全量 `tests/api/pi-*` 回归 → **96/96 通过**（含本新增套件与既有 reuse / SSE wire / hydrate / run 套件）。

### 8.4 三项后续优化实施记录（2026-09-04，同批落地）

**① 面板展示 inputs/ + 非图片内联预览**

- 面板：`PiChatNodeHost.fetchWorkspaceFiles` 带 `include_inputs=1`——「工作区文件」面板统一展示
  产物 + 上传文件；后端开关（§8.1）原样复用。
- 预览：新增 `FilePreviewModal`（`ChatNode` 的 `SkillFileCard` 接入，消息卡片与面板共用）——文本类
  （txt/md/json/csv/代码等，白名单扩展名）fetch → 截断渲染（React 转义无注入面；md 目前以源码文本
  预览，渲染化方案见 §9）；PDF → blob URL + `<iframe>` 走浏览器原生查看器；图片 → blob `<img>` 预览；
  其余格式提示下载。Esc / 点遮罩关闭。

**② 拖拽上传**

- 输入区 `onDragEnter/Over/Leave/Drop`（`Files` 类型判定 + enter/leave 成对计数防闪烁）；释放走与
  「选择文件」完全相同的通道（skill_agent = 任意格式 → `inputs/`，其他 = 图片校验 base64）；拖入高亮
  「松开以上传文件」浮层；生成中拒绝并提示。
- 窗口级 `dragover`/`drop` 仅对携带 `Files` 的事件 `preventDefault`（防拖到空白处触发浏览器导航）；
  节点拖拽是 pointer 事件（`CanvasNode` `onPointerDown` + `setPointerCapture`），互不干扰。

**③ 继承图片彻底路径化（data URL / 封面代理）**

- `/chat/import` 扩展：`data_urls`（data URL 图片，`decodeDataUrlImage` 解析 + 魔数校验）与豆瓣封面
  代理（`/api/modules/bookplate/cover?url=…` → `fetchCoverBytes`，内部限定 doubanio.com，SSRF 面受控）。
- 前端 `PiChatNodeHost.send` 分类：`/static/` + 封面代理 → `urls`；`data:` → `data_urls`；仅剩的外部
  URL（如豆瓣直链）走 base64 兜底；导入整体失败时整组降级 base64，不阻断对话。

### 8.5 测试与验证（三项优化后）

- `pi-file-attach.test.ts` 扩展至 **15 用例**：新增 data_urls 解码落盘 / 非图片魔数 400 / 封面代理非豆瓣
  域名 400。
- `npx tsc --noEmit`（backend）+ `npx tsc -b`（frontend）→ 0 错误；oxlint 无新增错误（既有告警不变）。
- 全量 `tests/api/pi-*` 回归 → **100/100 通过**。

### 8.6 遗留项

- ④ base64 常驻会话历史：上传/继承图片已全部路径化；`read` 工具读图仍会内联进工具结果（pi 协议绑定，未改）。
- 非图片格式（音频/视频）与 md 渲染化预览：优化方案见 §9，已立项待实施。

### 8.7 面板分组与对话层对称实施记录（2026-09-04，同批落地）

背景：§8.4① 把上传文件与产物混在单个扁平列表中，无法区分来源（只能靠 `inputs/` 前缀猜）。

**方案（A：单面板双分组折叠 + 对话层对称）**

- 数据层：`workspaceFiles.ts` 新增 `splitWorkspaceFiles`（`inputs/` 前缀分桶：产物 / 上传）与
  `extractUserUploadRefs`（从用户正文提取 `inputs/…` 路径 → 卡片数据，返回剔除路径后的展示文本）。
- 面板：新增 `WorkspaceFileGroup` 可折叠分组组件（标题 + 计数 + chevron + 卡片列表，空组整组隐藏），
  内联面板改为两组——「Agent 产物」默认展开、「我的上传」默认收起（带 accent 圆点微区分，分组本身已
  表达来源，颜色仅辅助）；刷新按钮与总计数保留。纯前端分桶，后端零改动。
- 对话 user 气泡：上传路径不再只是纯文本——提取为与 assistant 侧同款的可预览/下载卡片
  （复用 `SkillFileCard`），展示文本剔除路径段；发送给模型的原文不变（路径仍随正文送达 pi）。

**验证**

- `npx tsc -b`（frontend）→ 0 错误；oxlint 与改造前同为 6 个既有告警（`git stash` 基线核对），无新增。
- 后端零改动，无需回归（上一轮 100/100 仍有效）。

---

## 九、多格式文件预览优化方案（2026-09-06）

> 本段记录对 `FilePreviewModal` 预览能力的扩展方案（§8.6 遗留项「音频/视频无内联预览」的立项）。
> ①② 已按轻量方案实施（见 §9.5）；③④ 仍为按需预留。编码原则与前述章节一致（KISS、复用既有设施、不引重依赖）。

### 9.1 现状（2026-09-06 代码基线）

`FilePreviewModal.tsx`（消息卡片与「工作区文件」面板共用）按扩展名分发四类：

| 类型 | 实现 | 状态 |
| --- | --- | --- |
| markdown（md/markdown） | fetch → streamdown 渲染（React + shiki 高亮，无注入面） | ✅（§9.5） |
| text（txt/json/csv/代码等白名单） | fetch → 截断渲染（≤100k 字符，React 转义） | ✅ |
| pdf | blob URL + `<iframe>` 浏览器原生查看器 | ✅ |
| image（png/jpg/jpeg/gif/webp/svg） | blob URL + `<img>` 居中预览 | ✅ |
| audio（mp3/wav/ogg/m4a/aac/flac/opus 等） | blob URL + `<audio controls>`（>50MB 退化下载卡） | ✅（§9.5） |
| video（mp4/webm/mov/m4v/ogv 等） | blob URL + `<video controls>`（>50MB 退化下载卡） | ✅（§9.5） |
| 其余格式 | 仅下载卡片 | — |

关键约束不变：skill-files 接口要求 `Authorization` 头，`<audio>`/`<video>`/`<iframe>` 带不上，一切预览仍走
`fetchSkillFile`（fetch + authHeaders → blob → objectURL → 用毕 revoke）；blob 全量缓冲，objectURL 上无 HTTP Range 流式。

### 9.2 方案分层（零依赖优先，重框架按需）

**① md / 代码渲染化（零新依赖）**

- 复用项目已有 `streamdown`（`frontend/src/platform/utils/markdown.tsx`，chat 消息同款：React 渲染 + shiki
  高亮 + 消毒管线），替换当前 `<pre>` 源码文本展示；
- 保持 React 渲染（无原始 HTML 注入面，仅既有 mermaid SVG 例外）；大文件截断策略保留。

**② 音频 / 视频内联预览（零新依赖）**

- `previewKindOf` 增 audio/video 分支，渲染 `<audio controls>` / `<video controls>`，复用既有
  fetch → blob → objectURL 管线（卸载 revoke；加载失败态退化下载）；
- 体积阈值退化为下载卡（事件来源 `size` 已知时直接判；正文提取来源 `size=0` 时 fetch 后补读 `blob.size`）。

**③ PDF 增强（按需）**

- 现 iframe 原生查看器满足基本预览；若需弹层内翻页 / 搜索 / 缩略图，再按需动态引 `pdfjs-dist`（不进主包）。

**④ Office 格式（docx/xlsx/pptx；触发条件：inputs/ 上传出现这类文件）**

- 前端库方案：`docx-preview` / SheetJS / `@js-preview` 按格式各自引入；
- 一站式方案：**Open File Viewer**（`xushanpei/open-file-viewer`，Apache-2.0，纯前端 SDK，206 扩展名映射 /
  24 条预览链路，覆盖图片/音视频/Office/PDF/代码/MD，Vue/React/Svelte）——需要「一个组件覆盖全部业务附件」时整体评估。

### 9.3 结论与选型建议

- **不引入通用「万能预览框架」**：浏览器本身就是图片/音频/视频/PDF 的原生查看器，md/代码渲染已有
  streamdown + shiki；素材生成平台的产物（图、md、代码、pdf、短音视频）用 ①②③ 零依赖方案即可覆盖，
  重框架的收益集中在 Office/CAD/OFD 等浏览器不原生支持的格式。
- **服务端转码套件（kkFileView / ONLYOFFICE / LibreOffice headless）与单机部署不匹配**，不推荐——为预览
  引入 Java/转换服务不值得。
- Open File Viewer 留作「将来若需 Office 全家桶」的备选；集成前核对两点：① 接受 blob URL / ArrayBuffer
  （鉴权管线是 fetch → blob → objectURL）；② 按格式动态 import 控体积（pdf.js + office 渲染器较大）。

### 9.4 实施清单（①② 已完成，见 §9.5；③④ 按需）

1. ✅ `FilePreviewModal.tsx`：`previewKindOf` 增 markdown/audio/video；渲染分支扩展（`HeaderIcon` 随类型切换）
   —— 验证：`npx tsc -b`（frontend）0 错误
2. ✅ md 分支接入 streamdown（复用 `markdown.tsx` 导出 + `normalizeMarkdown`）—— 验证：tsc + oxlint 无新增
3. ✅ 两端 MIME 表同步：`file-utils.ts`（后端）与 `workspaceFiles.ts`（前端）的 `MIME_BY_EXT` 同口径扩充
   音频/视频扩展名 —— 验证：`npx tsc --noEmit`（backend）0 错误
4. ✅ 测试：`tests/api/skills.test.ts` 加 audio/video Content-Type 断言 —— 全量 `tests/api/pi-*` + skills
   回归 **143/143 通过**

### 9.5 实施记录（2026-09-06，轻量版）

> 按 §9.2 ①② 零依赖方案落地，未引入 pdf.js / Office 库 / Open File Viewer（③④ 触发条件未到）。

**`frontend/src/modules/bookplate/components/FilePreviewModal.tsx`**

- `previewKindOf` 增 `markdown` / `audio` / `video` 三类：md/markdown → streamdown 渲染（chat 消息同款
  React + shiki 高亮 + `normalizeMarkdown` 预处理，无原始 HTML 注入面）；音频（mp3/wav/ogg/m4a/aac/flac/
  opus/weba 等）→ blob URL + `<audio controls>`；视频（mp4/webm/mov/m4v/ogv 等）→ blob URL +
  `<video controls>`；头部图标随类型切换（Image / Music / Video / FileText）；`preload="metadata"` 避免媒体整读；
- 媒体体积阈值：已知 `size > 50MB` 退化为下载卡（blob 全量缓冲、objectURL 无 Range；正文提取来源 size=0
  视为未知走预览）；原有 text / pdf / image / binary 行为不变（md 从 text 拆出为 markdown，仅渲染方式变化）。

**MIME 表同步（两端同口径）**

- 后端 `file-utils.ts` 与前端 `workspaceFiles.ts` 的 `MIME_BY_EXT` 同步扩充：`ogg/oga`(audio/ogg)、
  `m4a`(audio/mp4)、`aac`、`flac`、`opus`、`weba`(audio/webm)、`webm`(video/webm)、`m4v`、`ogv`(video/ogg)；
  前端表扩充后正文 / inputs 路径提取同步识别这些扩展名（卡片 + 预览自动可用）。

**测试与验证**

- `skills.test.ts`「skill-files 下载」+2 断言：audio.mp3 → `audio/mpeg`、video.mp4 → `video/mp4` Content-Type。
- `npx tsc --noEmit`（backend）+ `npx tsc -b`（frontend）→ 0 错误；oxlint 无新增错误/告警
  （3 个既有 error 位于 vendored `watercolor/lib/brush.esm.js`，与本次改动无关）。
- 全量 `tests/api/pi-*.test.ts` + `tests/api/skills.test.ts` 回归 → **143/143 通过**。

**遗留**

- ③④（pdf.js 弹层内翻页 / Office 与 Open File Viewer）触发条件未到，按 §9.2 预留；
- md 预览内嵌相对路径图片时可能裂图（与 chat 正文提取口径一致的已知边界，需服务端把相对引用换算为
  skill-files URL 才能根治）。